<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/_fl_inner"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/_header_footer_top_bottom_padding"
    android:paddingLeft="@dimen/_header_footer_left_right_padding"
    android:paddingRight="@dimen/_header_footer_left_right_padding"
    android:paddingTop="@dimen/_header_footer_top_bottom_padding" >

    <ImageView
        android:id="@id/_pull_to_refresh_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:contentDescription="@null" />

    <ProgressBar
        android:id="@id/_pull_to_refresh_progress"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="gone" />

</FrameLayout>